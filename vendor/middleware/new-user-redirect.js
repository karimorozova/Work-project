export default ({app, store, redirect, route }) => {
    //  app.$axios.get("/vendor/has-competencies?token="+store.state.token).then((result) => {
    //   if (route.path !== '/competencies' && result.data.check) {
    //     redirect('/competencies')
    //   }
    // })
}
